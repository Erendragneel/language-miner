# Language Miner 6.4.188

Parent/Teacher Center refresh no longer replaces the student email form. Background polling pauses in the linking view and uses a 30-second interval elsewhere. Email drafts and focus survive in-flight refreshes, and failed submissions retain the address for retry. Successful requests still require a verified recipient-visible receipt.

Verified with a simulated cloud service; no real student requests sent during testing.
